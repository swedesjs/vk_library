part of vk_library_enums;

enum FilterAppsGetCatalog {
  /// Returns a list of installed applications (for mobile applications only).
  INSTALLED,

  /// Returns a list of applications installed in the `Editor's Choice` (for mobile applications only).
  FEATURED,
}
