part of vk_library_enums;

/// The type of friends list to create.
enum TypeAppsGetFriendsList {
  /// Available for invitation (do not play the game).
  INVITE,

  /// Available to send a request (already playing).
  REQUEST,
}
